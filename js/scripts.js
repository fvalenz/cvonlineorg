// //De esta forma se inicializa primero el doc de html y los scripts (forma vieja)
// document.addEventListener('DomContentLoaded', function (event) {
    
// });

// //De esta forma se inicializa primero el doc de html y los scripts (forma nueva)
// $(document).ready(function(){

// });

//De esta forma se inicializa primero el doc de html y los scripts (forma nueva corta)
$(function() {
    'use strict';
    
});